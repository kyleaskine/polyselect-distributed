# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Server runtime state (per-job)
*.db
*.db-wal
*.db-shm
polys/
jobdir/
token

# polylocal real config (the .example is checked in)
polylocal.ini
polylocal-work/

# Client runtime state
work/
run-client.sh
worktodo.ini
coeff_list.txt
*.ms
*.ms.zst
msieve.dat*

# Cloned/built deps on a client box
msieve-s/
